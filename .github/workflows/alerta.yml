name: Alerta DevOps

on:
  push:
    branches:
      - main

jobs:
  notificar:
    runs-on: ubuntu-latest

    steps:
      - name: Enviar notificación a ntfy
        run: |
          curl -d "Se realizó un push al repositorio Hola Mundo de GitHub" https://ntfy.sh/devops-itla
